<?xml version="1.0" encoding="utf-8"?>
<resources>

    <attr name="layoutCenter" format="boolean"/>
    <!--name为声明的"属性集合"名，可以随便取，但是最好是设置为跟我们的View一样的名称-->
    <declare-styleable name="Rounded">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="roundedColor" format="color" />
        <attr name="layoutCenter"/>
    </declare-styleable>

    <declare-styleable name="Round">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="roundColor" format="color" />
    </declare-styleable>

    <declare-styleable name="MessageBox">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="messageBoxColor" format="color" />
        <attr name="layoutCenter"/>
        <attr name="arrowDirection" format="string" />
    </declare-styleable>

</resources>