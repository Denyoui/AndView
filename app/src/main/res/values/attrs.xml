<?xml version="1.0" encoding="utf-8"?>
<resources>

    <attr name="layoutCenter" format="boolean"/>
    <!--name为声明的"属性集合"名，可以随便取，但是最好是设置为跟我们的View一样的名称-->
    <declare-styleable name="Rounded">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="roundedColor" format="color" />
        <attr name="layoutCenter"/>
    </declare-styleable>

    <declare-styleable name="Round">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="roundColor" format="color" />
    </declare-styleable>

    <declare-styleable name="MessageBox">
        <!--声明我们的属性，名称为default_size,取值类型为尺寸类型（dp,px等）-->
        <attr name="messageBoxColor" format="color" />
        <attr name="layoutCenter"/>
        <attr name="arrowDirection" format="string" />
    </declare-styleable>

    <declare-styleable name="AvatarImageBehavior">
        <attr name="finalYPosition" format="dimension"/>
        <attr name="startXPosition" format="dimension"/>
        <attr name="startToolbarPosition" format="dimension"/>
        <attr name="startHeight" format="dimension"/>
        <attr name="finalHeight" format="dimension"/>
    </declare-styleable>



    <!-- AvatarImageBehavior -->
    <declare-styleable name="AvatarBehavior">
        <attr name="finalYyPosition" format="dimension" />
        <attr name="startXxPosition" format="dimension" />
        <attr name="startToolbarPositions" format="dimension" />
        <attr name="startHeights" format="dimension" />
        <attr name="finalHeights" format="dimension" />
        <attr name="finalSize" format="dimension" />
        <attr name="finalXx" format="dimension" />
        <attr name="toolBarHeight" format="dimension" />
    </declare-styleable>

</resources>